# GitHub Data Repo (branch-based backend)
# Format: owner/repo — the same repo or a separate data repo
GITHUB_DATA_REPO=
# Personal access token for reads + registry writes (needs repo scope)
GITHUB_APP_TOKEN=

# NextAuth (GitHub OAuth)
AUTH_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
